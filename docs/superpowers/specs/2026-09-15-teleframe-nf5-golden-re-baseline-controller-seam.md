# CONTROLLER SURVIVAL SEAM verbatim #5 — Task 0's surgical Schema-golden re-baseline (stored at the compaction floor, controller-owned; re-read IN FULL first after every compaction alongside verbatims #1..#4 which all sit on disk in the same dir)

## Owner operative state (the true gate — never lose it)
The owner's real complaint, repeated across every verbatim: **"the tables in mysql is still the same shit" / "i still see the same old tables" / "the tables in mysql is still the same shit"** — the auto-generated NF5 mirror surface must be GONE from the generated tree AND the MySQL tables must ACTUALLY change to the hand-authored curated surface, proven live against the real seeded account. Owner: budgets are fake, unlimited AI — do not stop for budget; do not fabricate green; after EVERY compaction re-read ALL stored verbatims in full (protocol, non-negotiable).

## Task 0 (controller, LANDED + verified on disk this session)
- Purge commit f360e9bb pushed to feat/nf5-mirror-reverse-engineering: 851 generated mirror artifacts + 13 legacy tf_* migrations purged; SchemaRegenerator mirror() stage disabled; generator machinery (mirror stage) removed; manifest re-generated purged-reality.
- 4 controller-owned merges land domain/identity + domain/updates/media/messages (worktree topology verified: 4 teleframe-domain-* worktrees on domain/* branches).
- 3 reverse-engineering skills installed + verified at real paths: protocol-reverse-engineering, ghidra-headless, performing-sqlite-database-forensics.
- Verbatims #1..#4 all present on disk (22142b mtproto-reverse-eng, 12125b vision, 3301b clarify-controller-seam, 6943b clarification); re-read in full after every compaction.

## STILL RED — the honest remainder (not complete, never fabricated green)
- The 2 Schema goldens (RegenerationGoldenTest + ShipDialGoldenTest) STILL fail: last real phpunit = FAILURES (8 failures, 1 risky, ~14 tests). My surgical re-baseline from last window reported "applied 0/6 surgical re-points" — the exact-string pins I attempted did NOT match the files' current text, so the edits did NOT land. The controller must re-READ the exact failing pins from disk, THEN re-point them to purged reality (mirror ABSENT, app-owned curated tf_* migrations only). Not green. Do not claim.
- MySQL: THE TABLES ARE STILL UNCHANGED. Live proof + real seeded account verification NOT YET run on main. This is the owner's actual gate; it remains OPEN. [goal:continue] — controller resumes after compaction by re-reading all verbatims, then running the 2 goldens green, merging feature work to main, and proving live MySQL change against the real account.
